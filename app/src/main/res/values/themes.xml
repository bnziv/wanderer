<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Wanderer" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/primaryWhite</item>
        <item name="android:textColor">@color/primaryGreen</item>
    </style>
    <style name="Theme.Wanderer" parent="Base.Theme.Wanderer" />
</resources>